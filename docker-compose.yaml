# https://docs.docker.com/compose/compose-file/

version: "3.7"
services:
  prikka:
    build: ./
    image: prikka:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000/tcp"
    network_mode: host
